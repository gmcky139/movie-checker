# Codexへ渡す省利用量プロンプト

リポジトリのルートで、次の「実装指示」を1回だけ渡す。

## 実装指示

`AGENTS.md`と`docs/real_data_spec.md`に従い、TMDBポスター対応を未実装部分まで完成させて
ください。GitHub Actionsには`TMDB_API_READ_TOKEN`を登録済みです。

既存の3映画館取得、作品名正規化、静的Pages構成、UIを再利用し、TMDBによる保守的な
作品照合、ポスター表示、fallback、取得率検証、帰属表示、Actions Secret、Docker
BuildKit secret、offlineテストを一度の実装パスで追加してください。誤ったポスターを
採用せず、通常映画のポスター取得率70%以上を完成条件とします。

Codex利用量を節約するため、巨大な旧仕様やリポジトリ全体を繰り返し出力・要約せず、
`rg`と必要範囲だけで調査してください。最初の計画説明は5項目以内にし、そのまま実装、
focused test、最後のDocker検証、README更新まで進めてください。大規模リファクタ、UIの
再設計、無関係な修正、重複する自己レビューは不要です。

WSLホストでNode.js/npmを実行せず、Tokenを表示・保存・ログ出力しないでください。
commit、push、workflow実行は行わないでください。最後は変更ファイル、実行結果、TMDBの
一致数・未一致タイトル・取得率、未確認事項、人間側の作業だけを簡潔に報告してください。

## 完了報告後の扱い

Codexが上記タスク内でテストと自己レビューまで完了していれば、別の「再レビュー」指示は
原則として送らない。失敗または未確認項目が報告された場合だけ、対象を限定して次を送る。

```text
前回報告の未達成項目だけを修正してください。調査とテストをその項目に限定し、修正後に
必要なDocker検証だけを再実行してください。commitとpushは行わず、差分と結果を簡潔に
報告してください。
```
